export { AuthProvider, useAuth } from '@packages/shared/lib/auth/AuthProvider';

export { useAuthState } from '@packages/shared/lib/auth/useAuthState';

export type { AuthContextValue } from '@packages/shared/lib/auth/AuthProvider';

export type {
  UseAuthStateOptions,
  UseAuthStateReturn,
} from '@packages/shared/lib/auth/useAuthState';
