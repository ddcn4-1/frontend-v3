/**
 * Queue Entity
 * FSD v2.1: entities/queue/index.ts
 */

// Re-export types (will be added to shared/types)
// export type { QueueStatus, QueueEntry } from '@packages/shared';

// API services
export { queueService } from './api/queueService';
