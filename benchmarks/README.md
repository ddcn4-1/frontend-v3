# Build Performance Benchmarks

이 디렉토리는 GitHub Actions 빌드 성능 벤치마크 결과를 저장합니다.

## 📊 벤치마크 실행 방법

### 1. 🚀 한 번에 여러 설정 비교 (권장)

1. GitHub 저장소의 **Actions** 탭으로 이동
2. 왼쪽 사이드바에서 **Multi Benchmark Comparison** 워크플로우 선택
3. **Run workflow** 버튼 클릭
4. 입력 값 설정:
   - `configurations`: 실행할 벤치마크 조합
     - `all`: baseline, with-turbo, parallel-builds, all-optimizations (4가지)
     - `cache-only`: baseline vs with-turbo (캐시 효과만 비교)
     - `build-only`: baseline vs parallel-builds (병렬 빌드 효과만 비교)
   - `run_count`: 실행 횟수 (기본값: 3)
5. **Run workflow** 버튼 클릭하여 실행

**장점**:
- ✅ 한 번에 모든 조합 자동 테스트
- ✅ 자동으로 비교 리포트 생성
- ✅ baseline 대비 개선율 자동 계산
- ✅ 최적의 설정 자동 추천

### 2. 개별 벤치마크 실행 (세밀한 제어)

1. GitHub 저장소의 **Actions** 탭으로 이동
2. 왼쪽 사이드바에서 **Build Performance Benchmark** 워크플로우 선택
3. **Run workflow** 버튼 클릭
4. 입력 값 설정:
   - `benchmark_name`: 벤치마크 이름 (예: `baseline`, `optimized-v1`, `turbo-cache-enabled`)
   - `run_count`: 실행 횟수 (기본값: 3) - 평균값 계산을 위해 여러 번 실행
   - `include_deploy`: S3 배포 벤치마크 포함 여부
5. **Run workflow** 버튼 클릭하여 실행

### 3. 벤치마크 결과 확인

실행이 완료되면:

- **GitHub Step Summary**에서 즉시 결과 확인 가능
  - Multi Benchmark: 모든 설정의 비교표와 추천사항
  - Single Benchmark: 개별 실행 결과
- `benchmarks/` 디렉토리에 JSON 파일로 자동 저장됨
- Artifacts에서 상세 결과 다운로드 가능 (90일 보관)

## 📈 벤치마크 비교 방법

두 개의 벤치마크 결과를 비교하려면:

```bash
# 비교 스크립트 실행
node scripts/compare-benchmarks.js <baseline-id> <optimized-id>

# 예시
node scripts/compare-benchmarks.js baseline-20250124-120000 optimized-v1-20250124-130000
```

### 출력 예시

```
📊 Benchmark Comparison

Baseline:  baseline-20250124-120000
Optimized: optimized-v1-20250124-130000

Environment
  Runner: ubuntu-latest
  Node.js: 22
  pnpm: 9.15.0

Phase                | Before →  After | Change     | Time Saved
──────────────────────────────────────────────────────────────────────
  Checkout           |    5s →    5s | +0.0%      | +0s
  pnpm Setup         |    3s →    3s | +0.0%      | +0s
  Node.js Setup      |   12s →    8s | -33.3%     | +4s
  Dependencies       |  120s →   45s | -62.5%     | +75s
──────────────────────────────────────────────────────────────────────
Setup Total          |  140s →   61s | -56.4%     | +79s
──────────────────────────────────────────────────────────────────────
  Client Build       |   85s →   35s | -58.8%     | +50s
  Admin Build        |   78s →   32s | -59.0%     | +46s
  Accounts Build     |   72s →   30s | -58.3%     | +42s
──────────────────────────────────────────────────────────────────────
Build Total          |  235s →   97s | -58.7%     | +138s
──────────────────────────────────────────────────────────────────────
TOTAL                |  375s →  158s | -57.9%     | +217s
══════════════════════════════════════════════════════════════════════

📈 Summary

✓ Build 속도가 217초 (57.9%) 개선되었습니다!

주요 개선 영역:
  1. Build: 138s (58.7%) 개선
  2. Setup: 79s (56.4%) 개선
```

## 🎯 벤치마크 설정 설명

### 자동으로 실행되는 설정들

Multi Benchmark Comparison 워크플로우는 다음 설정들을 자동으로 실행하고 비교합니다:

| 설정 | Turbo Cache | 병렬 빌드 | 설명 |
|------|-------------|-----------|------|
| **baseline** | ❌ | ❌ | 최적화 전 현재 상태 |
| **with-turbo** | ✅ | ❌ | Turbo 원격 캐싱만 적용 |
| **parallel-builds** | ❌ | ✅ | 병렬 빌드만 적용 |
| **all-optimizations** | ✅ | ✅ | 모든 최적화 적용 |

### 설정별 기대 효과

1. **baseline** (기준선)
   - 최적화 전 현재 빌드 파이프라인의 성능 측정
   - 다른 설정과 비교하기 위한 기준점

2. **with-turbo** (Turbo 캐시)
   - Turbo 원격 캐싱으로 빌드 결과물 재사용
   - 기대 개선: 빌드 시간 50-70% 단축

3. **parallel-builds** (병렬 빌드)
   - client, admin, accounts 앱을 동시에 빌드
   - 기대 개선: 빌드 시간 40-60% 단축

4. **all-optimizations** (전체 최적화)
   - Turbo 캐시 + 병렬 빌드 조합
   - 기대 개선: 빌드 시간 60-80% 단축

## 📁 파일 구조

```
benchmarks/
├── README.md                        # 이 파일
├── index.json                       # 모든 벤치마크 결과 인덱스
├── baseline-20250124-120000.json   # 개별 벤치마크 결과
├── optimized-v1-20250124-130000.json
└── ...
```

### 벤치마크 결과 파일 구조

```json
{
  "benchmark_id": "baseline-20250124-120000",
  "benchmark_name": "baseline",
  "timestamp": "2025-01-24T12:00:00Z",
  "environment": {
    "runner": "ubuntu-latest",
    "node_version": "22",
    "pnpm_version": "9.15.0"
  },
  "results": [
    {
      "run_number": 1,
      "timings": {
        "checkout": 5,
        "pnpm_setup": 3,
        "node_setup": 12,
        "dependencies": 120,
        "setup_total": 140,
        "client_build": 85,
        "admin_build": 78,
        "accounts_build": 72,
        "build_total": 235,
        "total": 375
      }
    }
  ],
  "summary": {
    "avg_total_seconds": 375,
    "run_count": 3
  }
}
```

## 🔄 권장 워크플로우

### 방법 1: 빠른 전체 비교 (권장)

1. **Multi Benchmark Comparison 실행**
   ```bash
   Actions → Multi Benchmark Comparison → Run workflow
   configurations: all
   run_count: 3
   ```

2. **GitHub Step Summary에서 결과 확인**
   - 모든 설정의 성능 비교표
   - baseline 대비 개선율
   - 최적 설정 추천

3. **최적 설정 선택 및 적용**

### 방법 2: 단계별 최적화

1. **Baseline 측정**
   ```bash
   Actions → Build Performance Benchmark → Run workflow
   benchmark_name: baseline
   run_count: 3
   ```

2. **최적화 적용**
   - 코드 변경 및 워크플로우 수정

3. **최적화 후 측정**
   ```bash
   Actions → Build Performance Benchmark → Run workflow
   benchmark_name: optimized-v1
   run_count: 3
   ```

4. **결과 비교**
   ```bash
   node scripts/compare-benchmarks.js baseline-YYYYMMDD-HHMMSS optimized-v1-YYYYMMDD-HHMMSS
   ```

5. **성능 개선 확인 및 문서화**

## 📝 주의사항

- 벤치마크는 GitHub Actions runner의 상태에 따라 변동될 수 있습니다
- 정확한 비교를 위해 최소 3회 이상 실행하여 평균값을 사용하세요
- 같은 시간대에 측정하면 더 정확한 비교가 가능합니다
- Cold cache vs Warm cache 상태를 고려하세요

## 🎯 목표 성능

| 단계 | 목표 개선률 | 예상 시간 절감 |
|------|------------|---------------|
| 병렬 빌드 | 40-60% | 2-3분 |
| Turbo 캐시 | 50-70% | 3-4분 |
| 종속성 캐싱 | 30-40% | 1-2분 |
| 전체 최적화 | 60-80% | 4-6분 |

## 📞 문의

벤치마크 관련 문의사항이나 개선 제안은 이슈를 생성해주세요.
